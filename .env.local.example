# Database path (defaults to ./expenses.db in project root)
# Use :memory: for in-memory database (e.g., in tests)
DB_PATH=./expenses.db

# Node environment (development, production, test)
NODE_ENV=development
